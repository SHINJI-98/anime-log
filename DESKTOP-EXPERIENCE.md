# 我的追番：桌面体验交付与验证

## 范围与基线

- 独立工作树：`C:\Users\A\.codex\worktrees\efe9\anime-log`。
- 分支：`codex/desktop-experience`，基于 `9fbcf12`，包含 Windows 自启动路径引用和命名登录项读取修复。
- 保留 Electron + Vue、sql.js/SQLite、`anime-log://local/api`。没有增加 Java、HTTP 服务、播放器或账号系统。
- 原工作区未修改。运行测试产生的依赖、构建产物和隔离数据库不纳入源码提交。

## 分阶段实现

| 阶段 | 实现 | 主要提交 |
| --- | --- | --- |
| 1 | 悬停等待约 250ms；离开约 650ms 收起；点击保持，外部失焦/Esc 收起；拖动阈值 6 DIP；双击/菜单打开主界面；保留页面与草稿；统一托盘菜单；可选关闭后保留悬浮球 | `9e30521` |
| 2 | 保存显示器 ID 与工作区相对坐标；12 DIP 边缘吸附；四角展开与工作区限制；显示变化后恢复；锁定和重置位置 | `5cffa67` |
| 3 | 今天/本周视图与选择记忆；空状态；“安排播出”文案；三档尺寸；球与面板背景独立不透明度；即时 +1、失败回退、6 秒撤销；失败封面占位；刷新保留旧内容 | `599e027` |
| 4 | `--autostart` 区分登录与手动启动；安静显示球或仅托盘；保留手动模式；单实例恢复；Windows 原生全屏检测与退出恢复；唤醒后检查位置 | `acf5c7a`、`7bc999e` |
| 5 | 球模式启动不预加载完整界面；主进程明确通知可见性；周历隐藏时停止时钟与渲染，首次展开读取数据；托盘停止悬停轮询及全屏检测进程；100 次切换和 30 分钟采样脚本 | `a7d67ec` |

随后提交补充小工作区恢复、全屏期间托盘菜单、错误页中文标题、拖动取消与草稿回归覆盖。

`73e875c` 修复 Windows 原生最大化：主窗口不使用 Electron 的 `transparent: true`，周历模式在 Windows 11 22H2 及以上使用原生 acrylic 背景，完整界面恢复普通窗口。更旧的 Windows 使用实色背景，设置中禁用周历背景不透明度并说明原因。悬浮球仍支持独立不透明度。

## 可运行文件

- 第一批：`frontend/release/Anime-Log-stage1-2-portable.exe`
  - SHA-256：`D7CD1AD1DB4BB89B1D4ED5B940475C5DEB9BFB19129A9CC1CDD2C7D68AEBFB76`
- 最终版：[Anime-Log-0.1.0-portable.exe](frontend/release/Anime-Log-0.1.0-portable.exe)，75,876,983 字节，2026-09-16 构建，应用代码版本 `73e875c`（其后的提交仅为验证脚本与报告）。
  - SHA-256：`5263E6F73B706072ABCE631BD0EF9F83052DBC6A5C6460F27D96AE868DFF6291`
- 便携版仍使用用户数据目录保存个人 SQLite 与桌面设置；发布包不包含个人数据库。

## 使用方式

1. 运行最终便携 EXE。在完整界面点击“桌面便签”进入悬浮球模式。
2. 悬停片刻展开周历；移开后自动收起。单击球保持展开，点击外部或按 Esc 收起；双击球回到完整界面。
3. 拖动悬浮球调整位置。右键球或托盘可锁定、重置、显示/隐藏球，或打开完整界面。
4. 在设置中选择关闭行为、大小、不透明度和自启动方式。默认关闭是收进托盘；退出应用请使用托盘“退出”或修改关闭行为。
5. 周历可切换“今天/本周”，点击“+1”记录进度，6 秒内可撤销。编辑中的笔记会随完整界面保留；关闭整个应用前仍需保存笔记。

免安装文件移动位置后，重新关闭并开启“开机自启动”，更新 Windows 中登记的路径。应用不要求管理员权限。

## 验证分层

### 自动测试

- `npm.cmd run test:e2e`：构建、资源引用、主进程测试、Electron 窗口与业务流程。
- `tests/window-position.test.cjs`：四角、负坐标、不同显示器排列、100/125/150/200% 对应 DIP 工作区、断屏回退与损坏位置数据。
- `tests/ball-input.test.cjs`：小幅手抖仍视为点击，超过阈值仅拖动，取消指针不误点击，双击/右键独立动作。
- `tests/e2e/electron-smoke.spec.cjs`：追番、进度、失败回退、撤销、笔记、悬停、点击保持、Esc、弹窗暂停收起、关闭、重启、位置持久化和锁定。
- `tests/e2e/startup-fullscreen.spec.cjs`：安静启动、保存的手动模式、延迟加载完整界面，以及独立真实 Windows 全屏窗口的隐藏/恢复。
- `npm.cmd run test:packaged`：打包应用在无 Java PATH、8080 不可用时启动；SQLite、悬浮球、设置可用；检查包内不存在数据库、JAR 和 Vite 开发依赖。
- `npm.cmd run test:startup:windows`：用独立登录项名称写入并读取真实 Windows 注册表，核验外层便携 exe、引用路径及 `--autostart`，然后清理。Electron 返回的 `launchItems.args` 在本机为空，因此参数以实际注册表为准。
- `node tests/portable-launch.cjs`：直接运行最终外层便携 EXE，验证参数转发、静默自启动、完整界面延迟加载和隔离 SQLite 数据目录。
- 2026-09-16 最终结果：15 项单元/服务测试、6 项 Electron 流程测试、打包冒烟、Windows 注册表自启动和外层便携 EXE 检查均通过。完整界面的原生最大化、未保存笔记草稿与滚动恢复均有回归覆盖。

### 封面加载与界面检查

`node tests/desktop-image-performance.cjs` 使用隔离数据库创建 210 条追番记录，每天 30 条。测试通过 Electron 原生协议提供确定的 PNG 数据，避免依赖真实 CDN。隐藏时图片请求为 **0**；首次展开为 **77**；连续收起/展开 5 次后仍为 **77**。这验证了懒加载及保留 DOM 的复用，不代表网络延迟、HTTP 缓存或大图片压力的测量。

[原始计数](docs/desktop-validation/image-loading.json)。已检查[本周](frontend/release/validation/week.png)、[今天](frontend/release/validation/today.png)和[设置](frontend/release/validation/settings.png)的渲染截图，中文、长标题截断与设置滚动正常。原生窗口缩略图未被系统捕获，因此这些截图不能作为桌面背景透出效果的证据。

### 本机真实系统验证

- Windows 11 构建 `26200`；Electron `33.4.11`。
- 当前主屏为 3840×2160，Electron 报告缩放约 217.5%，工作区为 1766×960 DIP。
- 真正创建了独立的 Windows 全屏窗口，验证悬浮球隐藏、退出全屏后恢复，设置文件中的模式未改写。
- 原生坐标在此缩放下有数个 DIP 的取整差异；拖动结束只调整坐标，避免循环读写宽高导致尺寸漂移。

### 尚未实测的验收项

- 实体副屏拔插、混合 DPI 多屏拖动、远程桌面切换；这些目前只有坐标算法及显示事件测试。
- 真实 Windows 登录启动与休眠/唤醒过程；注册表和唤醒事件代码测试不能替代这些。
- 不同任务栏布局、全部实体 100/125/150/200% 设置、独占全屏游戏、多虚拟桌面组合。
- 长时间大量追番与真实 CDN 封面加载压力；210 条模拟数据检查和空数据库驻留测试不能证明这些负载的上限。

## 全屏检测的实际范围

悬浮球启用时运行一个隐藏的 Windows PowerShell 辅助进程，每秒读取可见顶层窗口边界。它忽略本应用、任务栏、最小化和 cloaked 窗口，并排除被较大前台窗口覆盖的背景全屏窗口。只隐藏命中显示器上的组件；完整界面和托盘状态停止辅助进程。检测失败时保留主界面与托盘入口，设置页显示错误详情。

边界信息使用 [Microsoft DWM 窗口属性](https://learn.microsoft.com/en-us/windows/win32/api/dwmapi/ne-dwmapi-dwmwindowattribute)。这是基于可见窗口几何的检测，不代表已验证所有独占游戏或覆盖层软件。

## 性能测量方法与结果

运行命令（在 `frontend`）：

```powershell
$env:ANIME_LOG_SOAK_MINUTES='30'
$env:ANIME_LOG_PERF_OUTPUT='../docs/desktop-validation/performance-soak.json'
node tests/desktop-performance.cjs
```

脚本使用隔离空数据库，记录 Electron 各进程工作集之和、CPU、JS 堆、DOM 节点、活动 interval 数量、图片请求条目、全屏辅助进程工作集及累计 CPU 时间。100 次展开收起的延迟包含 IPC 和两次动画帧等待；它不包含 250ms 悬停触发延迟。为防止桌面上已有全屏程序改变采样状态，仅在该基准测试内把检测结果映射至远离屏幕的位置，原生检测进程仍正常运行。

注意：进程工作集之和包含共享内存，不能直接视为独占物理内存；空数据的图片请求数为 0，不构成图片压力测试。辅助进程的成本单列。基线为第一批阶段 1、2 打包版，最终版增加原生背景和全屏检测，因此不能仅据隐藏轮询减少宣称总内存下降。

第一轮 30 分钟驻留已完成，记录在 [原生最大化修复前的采样](docs/desktop-validation/performance-pre-acrylic.json)。它使用旧透明窗口实现，只作历史记录，不作为最终包的长期结果。最终包已另行完成相同脚本的 30 分钟测试。

### 相同测量脚本的基线比较

以下为启动并打开过完整界面后的工作集总和，单位 MiB；不是仅球模式冷启动内存。

| 状态 | 阶段 1、2 基线：Electron | 最终包：Electron | 最终包：全屏辅助进程 |
| --- | ---: | ---: | ---: |
| 球可见、周历隐藏 | 472.0 | 478.0 | 80.2 |
| 仅托盘 | 471.8 | 469.7 | 0 |
| 周历展开 | 472.9 | 480.0 | 80.1 |
| 100 次收起/展开后 | 467.7 | 518.2 | 80.1 |

100 次展开等待：基线平均 **16.21ms**、最大 **17.35ms**；最终包平均 **16.24ms**、最大 **22.80ms**。这是一次本机采样，不构成严格统计显著性比较。

隐藏周历和托盘时，渲染页活动 interval 从基线 **1** 降至 **0**。最终包仅托盘时停止全屏辅助进程；球或周历运行时仍承担其成本。因此本次改进证明了隐藏工作减少，并未证明总驻留内存普遍下降。[基线原始数据](docs/desktop-validation/performance-baseline.json)。

### 最终包 30 分钟驻留

2026-09-16 22:00–22:30（北京时间）运行完成，退出码 0。[完整原始数据](docs/desktop-validation/performance-soak.json)含 4 个初始状态样本和 30 个每分钟样本，`complete: true`。

- 第 1–6 分钟周历可见；第 7 分钟因失焦收起，此后 24 个样本为隐藏周历、悬浮球运行状态。这是混合状态驻留，不能表述为最终版周历持续展开 30 分钟。
- Electron 工作集总和范围 **500.6–520.3MiB**，平均 **511.0MiB**，最后 **515.1MiB**。
- Electron 进程 CPU 采样之和平均 **0.0329%**，最大 **0.2828%**；不包含辅助进程。
- 全屏辅助进程最后工作集 **86.9MiB**，累计 CPU 时间 **11.25 秒**（包含该辅助进程启动以来的工作）。
- 所有驻留样本均为 **5 个 Electron 进程、97 个 DOM 节点**。可见样本有 **1** 个周历 interval，隐藏样本为 **0**。
- JS 堆由第 1 分钟约 **2.64MiB** 到第 30 分钟约 **3.07MiB**。没有进程、节点或定时器累积；工作集有波动，JS 堆有小幅增长，不能据此断言完全没有泄漏。

结论：本机验证了隐藏时停止周历时钟、托盘停止全屏辅助进程、封面懒加载和重复展开复用。应用仍有 Electron 运行时及全屏检测成本；没有宣称整体内存低于阶段 1、2 基线。真实多屏、登录/休眠与长期大数据负载仍按上文列为未实测。
